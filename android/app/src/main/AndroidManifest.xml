<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:label="disponere"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:label">
        <!-- Kein android:taskAffinity="" mehr.
             Die Flutter-Vorlage setzt es, aber AppAuths RedirectUriReceiverActivity
             und AuthorizationManagementActivity haben die Standard-Affinitaet
             (= Paketname). Zwei verschiedene Affinitaeten = zwei Tasks: der
             Rueckkanal landet dann nicht in unserem Task und die App kommt nach
             dem Login nicht nach vorn. Siehe flutter_appauth-Doku, Abschnitt
             Android. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Empfangsziel fuer geteilte Inhalte (Teilen & Bilder, Feature 2
                 + Bild-Nachzug). Text/URL kommen als ACTION_SEND mit text/plain,
                 Bilder als ACTION_SEND mit image/* (Einzelbild; eine etwaige
                 Bildunterschrift in EXTRA_TEXT wird mitgenommen). Alles laeuft
                 ueber den nativen Kanal (MainActivity) und landet im heutigen
                 Journal. Bewusst KEIN ACTION_SEND_MULTIPLE: das Eintrags-Sheet
                 fuehrt genau ein Bild; Mehrbild-Empfang waere ein spaeterer
                 Nachzug zusammen mit einem Mehrbild-Sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>